apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: worker-app-pod
      image: kodekloud/examplevotingapp_worker:v1

# kubectl create -f worker-app-pod.yaml


# to check use: 
# kubectl get pods 
